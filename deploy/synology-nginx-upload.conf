# Synology DSM – nginx limit pro upload LAZ (stovky MB)
# Zkopírujte na NAS: /etc/nginx/conf.d/proxy_podkladarna.conf
# Pak: sudo nginx -t && sudo synosystemctl restart nginx

client_max_body_size 0;
proxy_read_timeout 3600s;
proxy_send_timeout 3600s;
proxy_request_buffering off;
